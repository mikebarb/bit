                      <div class="student <%= "fs" + student.id.to_s
                        %><%= ' s-status-' + set_class_status_f(thisrole) 
                        %><%= ' s-kind-'   + set_class_kind_f(  thisrole) 
                        %><%= ' '          + set_class_run_f(   thisrole) 
                        %>" id=<%= slot + "n" + lesson.to_s + "s" + student.id.to_s %> >
                        <div style=persongroup>
                          <div class="studentname studentinline p-name"><%= student.pname %><%=
                               student.year == nil ? "   " : student.year.rjust(3)
                               %><%=student.sex == nil ? "" :
                                student.sex.downcase.include?("female")?
                                " (F) " : student.sex.downcase.include?("male")?
                                " (M) " : "" 
                                %> (stu)</div>
                          <div class="studentcomment comment studentinline ">
                            <% #thisrole = entry.roles.where(student_id: student.id).first %>
                            <div class="studentsubjects p-study"><%= student.study %></div>
                            <div class="rolecomment np-comment"><%= thisrole.comment %></div>
                            <div class="studentcommentdetail p-comment"><%= student.comment %></div>
                            <div class="statusinfo">
                              <div class="np-status">Status: <%= thisrole.status %></div>
                              <div class="np-kind">Kind: <%= thisrole.kind %></div>
                              <div class="p-status">Detail: <%= student.status %></div>
                            </div>
                          </div>
                        </div>
                      </div>
